ignore:
  # CVE-2025-7709: Integer overflow in SQLite FTS5 extension
  # Medium severity vulnerability in libsqlite3-0 version 3.46.1-7
  # No fix available in Debian Trixie - marked as "wont-fix"
  - vulnerability: "CVE-2025-7709"
    package:
      name: "libsqlite3-0"
    fix-state: "wont-fix"

  # GHSA-jwp6-cvj8-fw65: ReDoS in Spark UI log viewer
  # High severity in spark-core_2.12 3.5.3, fixed in 3.5.7
  # Ignoring until Spark dependency is upgraded
  - vulnerability: "GHSA-jwp6-cvj8-fw65"
    package:
      name: "spark-core_2.12"

  # GHSA-rwm7-x88c-3g2p: netty-transport-native-epoll half-closure DoS
  # Only affects Netty 4.2.x and 5.0.x — 4.1.x is not vulnerable.
  # Grype incorrectly flags 4.1.x because the advisory's "fixed in" points to 4.2.13.Final.
  # See: https://github.com/advisories/GHSA-rwm7-x88c-3g2p
  # See: https://github.com/netty/netty/pull/16689#issuecomment-4394849137 (author confirms 4.1.x unaffected)
  - vulnerability: "GHSA-rwm7-x88c-3g2p"
    package:
      name: "netty-transport-native-epoll"

  # Curl CVE exceptions - required for fetcher container functionality
  # Used for Amazon Corretto key download, Google Cloud Profiler download, and healthcheck
  - package:
      name: "curl"
      type: "deb"
  - package:
      name: "libcurl4t64"
      type: "deb"
